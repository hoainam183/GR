[pytest]
testpaths = tests
addopts = --tb=short -q
markers =
    integration: tests requiring external services (Qdrant, Elasticsearch, local models, or Tavily)
    e2e: full end-to-end tests across routing and retrieval flows
filterwarnings =
    ignore::DeprecationWarning:builtins:
